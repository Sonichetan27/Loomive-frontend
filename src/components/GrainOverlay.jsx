// A faint animated texture layered over the whole site — like the nap of
// woven fabric catching the light. Fixed, non-interactive, always behind content.
const GrainOverlay = () => <div className="grain-overlay" aria-hidden="true" />;

export default GrainOverlay;
